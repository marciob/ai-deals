# Optional: Escrow timeout in seconds (default: 86400 = 24h)
ESCROW_TIMEOUT_SECONDS=86400

# Deployer private key (never commit .env with real keys)
PRIVATE_KEY=0x...

# RPC URL for Monad mainnet
RPC_URL=https://rpc.monad.xyz
